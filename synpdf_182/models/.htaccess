Options -Indexes

<IfModule mod_mime.c>
    AddType application/octet-stream .onnx
</IfModule>

<IfModule mod_headers.c>
    <FilesMatch "\.(js|json|onnx)$">
        Header always set Cross-Origin-Resource-Policy "same-origin"
        Header always set Cross-Origin-Embedder-Policy "require-corp"
    </FilesMatch>
</IfModule>
